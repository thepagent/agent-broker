name: Chart Tests

on:
  pull_request:
    paths:
      - "charts/**"

jobs:
  helm-unittest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: helm-unittest/helm-unittest-action@v2
        with:
          helm-version: v3.17.3
          charts: charts/openab
