# 交接紀錄 2026-04-14

## 已完成
- **Spectra copilot-mcp-ecosystem**: Phase 1 (6/6), Phase 2 (5/8, 2.6-2.8 BLOCKED), Phase 4 (4/6)
- **MCP 接入**: COPILX GitHub MCP ✅, GITX SDK 注入失敗（MCP 只在 CLI 層）
- **Channel 鎖定**: 5 bot 各自 channel，不再搶 thread
- **bat loop 互斥鎖**: `9>"%LOCKFILE%"` FD 鎖，全 5 bat 套用
- **Copilot quota fix**: `0/1 used` → `0 premium reqs used`
- **Per-model breakdown**: `enrich_session_summary_with_per_model()` 替換 aggregate
- **`/mcp-browse` `/mcp-install` `/mcp-status`**: 已寫入 discord.rs，GITX 33 commands
- **`/soul` `/mcp-add` `/mcp-remove` `/mcp-list`**: 已部署
- **usage.rs `render_template` fallback**: 未解析 `{{ }}` → `N/A`
- **七海人設 `fs.readFileSync`**: 修復 bat `set /p` 1023 截斷
- **protocol.rs**: 加 `usage_update`/`context_update` info log（待 build）
- **Cargo.toml**: 加 `futures = "0.3"`（修 usage.rs 編譯）
- **connection.rs**: `#[cfg(unix)]` 保護 `setpgid`/`kill`/`pre_exec`

## 未完成（需修復才能 build）
1. **pool.rs 自訂方法被 upstream merge 覆蓋**:
   - `cached_models()` — 回傳快取的 model list
   - `cached_current_model()` — 回傳當前 model
   - `cached_native_commands()` — 回傳 native agent commands
   - 這些方法被 discord.rs 的 `/model` autocomplete、`/native` 等 handler 使用
   - **修法**: 在 pool.rs 重新加入這些方法（參考 git stash 或 `4feb202` commit）

2. **`cargo check` 17 errors**: 全部來自 pool.rs 缺少的方法 + type annotation

## 待測試
- **usage_update 支援**: protocol.rs 已加 info log，build 後各 backend 發訊息看 log 有無 `usage/context update received`
- **per-model breakdown**: 多 model session 後看 session summary 替換效果
- **`/mcp-browse`**: Discord 測試 11 個 registry servers 顯示

## 重要決策
- SDK 的 `mcpServers`/`configDir`/`cliArgs` 三種 MCP 注入全部不生效，MCP 只在 CLI 層
- GITX = 人設型（bridge），COPILX = 工具型（CLI + GitHub MCP）
- Phase 2-3 BLOCKED，per-user MCP 需架構級改動（OpenAB Rust 端支援 per-user 動態 agent args）
